#include <stdio.h>
int main() 
{
int a,b,c;
printf("Enter three numbers:");
scanf("%d%d%d",&a,&b,&c);
if(a<=b && a<=c)
printf("%d is the smallest.",a);
else if(b<=c)
printf("%d id the smallest.",b);
else
printf("%d id the smallest.",c);

return 0;
}
