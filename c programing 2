#include <stdio.h>
int main() 
{
int a,b,c;
printf("Enter three numbers:");
scanf("%d%d%d",&a,&b,&c);
if(a>=b && a>=c)
printf("%d is the largest.",a);
else if(b>=c)
printf("%d id the largest.",b);
else
printf("%d id the largest.",c);

return 0;
}
